# Быстрый старт

Пошаговая инструкция для быстрого запуска Learning Platform и решения типичных проблем.

## ⚡ Самый быстрый способ запуска

### 1. Запустите все сервисы через Docker Compose

```bash
docker compose up -d
```

Дождитесь запуска всех контейнеров (обычно 30-60 секунд).

### 2. Миграции применяются автоматически! ✅

При первом запуске API автоматически:
- Применяет все pending миграции базы данных
- Создает начальных пользователей

**Начальные пользователи:**
- **Студент**: `tst@tst.com` / `qweasd`
- **Инструктор**: `inst@inst.com` / `qweasd`

### 3. Откройте в браузере

- **API Swagger**: http://localhost:8082/swagger (или другой порт, если вы его изменили)
- **Blazor Client**: http://localhost:5002

### 4. Войдите в систему

Используйте начальные учетные данные или зарегистрируйтесь как новый студент.

Готово! 🎉

## 📝 Пошаговая инструкция

### Шаг 1: Запуск PostgreSQL

```bash
# Запустить только PostgreSQL
docker compose up -d postgres

# Дождаться готовности (обычно 10-20 секунд)
docker compose ps postgres
```

### Шаг 2: Применение миграций

**✅ Миграции применяются автоматически при запуске API!**

При первом запуске API автоматически применяет все pending миграции и создает начальных пользователей.

**Начальные пользователи:**
- **Студент**: `tst@tst.com` / `qweasd`
- **Инструктор**: `inst@inst.com` / `qweasd`

Проверить успешность миграций:
```bash
docker compose logs api | grep -i "migration\|seed"
```

Или проверить таблицы в БД:
```bash
docker compose exec postgres psql -U postgres -d learning_platform -c "\dt"
```

Должны отображаться таблицы: `Users`, `Courses`, `Assignments`, `Submissions`, `CourseMemberships`, `Teams`, `TeamMemberships`, `__EFMigrationsHistory`.

**Если миграции не применились автоматически**, примените их вручную:
```bash
# Установить EF Core Tools (если еще не установлены)
dotnet tool install --global dotnet-ef

# Добавить в PATH
export PATH="$PATH:$HOME/.dotnet/tools"

# Применить миграции
dotnet ef database update --project LearningPlatform.Data --startup-project LearningPlatform.API
```

**Важно:** При выполнении миграций вы можете увидеть сообщение об ошибке `HostAbortedException` - это нормальное поведение, миграции применяются успешно.

### Шаг 3: Запуск API

#### Вариант A: Через Docker (рекомендуется)

```bash
docker compose up -d api
```

API будет доступен на http://localhost:8080

#### Вариант B: Локально

```bash
cd LearningPlatform.API
dotnet run
```

API будет доступен на http://localhost:5281 (или порт из launchSettings.json)

### Шаг 4: Запуск Blazor Client

#### Вариант A: Через Docker

```bash
docker compose up -d client
```

Client будет доступен на http://localhost:5002

#### Вариант B: Локально

```bash
cd LearningPlatform.Client
dotnet run
```

Client будет доступен на http://localhost:5002

**Важно:** Если запускаете локально, убедитесь, что `ApiBaseUrl` в `appsettings.json` указывает на правильный адрес API:
- Docker: `http://localhost:8080`
- Локально: `http://localhost:5281`

### Шаг 5: Проверка работоспособности

1. **Проверьте API:**
   - Откройте http://localhost:8080/swagger (или http://localhost:5281/swagger при локальном запуске)
   - Должен открыться Swagger UI с документацией API

2. **Проверьте Blazor Client:**
   - Откройте http://localhost:5002
   - Должна открыться главная страница с кнопками "Sign In" и "Get Started"

## 🧪 Тестирование функциональности

### 1. Регистрация и вход

1. Откройте http://localhost:5002
2. Нажмите "Get Started" или перейдите на `/register`
3. Зарегистрируйтесь как студент (Role: Student)
4. Войдите в систему через `/login`

### 2. Создание курса (Instructor)

1. Зарегистрируйтесь с Role: Instructor (или измените роль в БД)
2. Войдите в систему
3. Нажмите "Create Course" в навигационной панели
4. Заполните форму создания курса
5. Курс будет создан с уникальным кодом для присоединения

### 3. Присоединение к курсу (Student)

1. Войдите как студент
2. Перейдите на страницу курсов
3. Введите код курса для присоединения
4. Вы будете добавлены в курс

### 4. Создание задания (Instructor)

1. Откройте детали курса
2. Нажмите "Create Assignment"
3. Заполните форму (название, описание, дедлайн)
4. Опционально: выберите команду для назначения задания
5. Задание будет создано

### 5. Сдача работы (Student)

1. Откройте детали задания
2. Введите ссылку на вашу работу (GitHub, Google Drive и т.д.)
3. Нажмите "Submit Assignment"
4. Работа будет отправлена

### 6. Оценка работы (Instructor)

1. Откройте детали задания
2. В списке сдач нажмите "Grade" или "Update Grade"
3. Введите оценку (0-100) и комментарий
4. Нажмите "Submit Grade"
5. После оценки вы будете перенаправлены на страницу курса

### 7. Управление командами (Instructor)

1. Откройте детали курса
2. Нажмите "Create Team"
3. Заполните форму (название, выберите студентов)
4. Команда будет создана
5. Вы можете добавлять/удалять участников команды

## ⚠️ Решение проблем

### Проблема 1: Ошибка "relation 'Users' does not exist" или миграции не применяются

**Симптомы:**
- При попытке зарегистрироваться или войти получаете ошибку: `42P01: relation "Users" does not exist`
- API работает, но таблицы в БД отсутствуют

**Решение:**

1. **Проверьте логи API:**
   ```bash
   docker compose logs api --tail=50
   ```
   Должны быть строки: "Applying database migrations..." и "Database migrations applied successfully"

2. **Если миграции не применились автоматически**, примените их вручную:
   ```bash
   # Убедитесь, что EF Core Tools установлены
   dotnet tool install --global dotnet-ef
   export PATH="$PATH:$HOME/.dotnet/tools"
   
   # Примените миграции
   dotnet ef database update --project LearningPlatform.Data --startup-project LearningPlatform.API
   ```

3. **Проверьте, что миграции применились:**
   ```bash
   docker compose exec postgres psql -U postgres -d learning_platform -c "\dt"
   ```
   Должны отображаться таблицы: `Users`, `Courses`, `Assignments`, `Submissions`, `CourseMemberships`, `Teams`, `TeamMemberships`, `__EFMigrationsHistory`.

4. **Перезапустите API:**
   ```bash
   docker compose restart api
   ```

### Проблема 1a: HostAbortedException при ручном применении миграций

**Это нормальное поведение, не ошибка!**

При выполнении `dotnet ef database update` вы можете увидеть:
```
[FTL] Application startup failed
Microsoft.Extensions.Hosting.HostAbortedException: The host was aborted.
```

Это ожидаемое поведение. EF Core Design-time tools:
1. Запускают приложение для получения конфигурации (connection string, настройки)
2. Получают необходимую информацию
3. Прерывают выполнение приложения (HostAbortedException)
4. Применяют миграции к базе данных

**Миграции применяются успешно**, несмотря на сообщение об ошибке.

### Проблема 2: .NET 8.0 runtime не найден

Если у вас установлен только .NET 10.0, используйте переменную окружения `DOTNET_ROLL_FORWARD=Major`:

```bash
# Для миграций
export PATH="$PATH:$HOME/.dotnet/tools"
DOTNET_ROLL_FORWARD=Major dotnet ef database update --project LearningPlatform.Data --startup-project LearningPlatform.API

# Для запуска клиента
DOTNET_ROLL_FORWARD=Major dotnet run --project LearningPlatform.Client
```

Или установите .NET 8.0 SDK: https://dotnet.microsoft.com/download/dotnet/8.0

### Проблема 3: Формы не отправляются, кнопки не работают

**Причины и решения:**

1. **Компонент не использует InteractiveServer:**
   - Убедитесь, что компонент имеет `@rendermode InteractiveServer`
   - Проверьте, что в `App.razor` настроен правильный render mode

2. **Ошибки JavaScript:**
   - Откройте консоль браузера (F12)
   - Проверьте наличие ошибок JavaScript
   - Убедитесь, что используется Blazor Server (не WebAssembly)

3. **Проблемы с подключением к SignalR:**
   - Проверьте, что Client может подключиться к серверу
   - Проверьте логи в консоли браузера

### Проблема 4: Не могу подключиться к API

**Проверьте:**

1. **API запущен:**
   ```bash
   docker compose ps api
   # или
   curl http://localhost:8080
   ```

2. **Правильный ApiBaseUrl:**
   - Откройте `LearningPlatform.Client/appsettings.json`
   - Убедитесь, что `ApiBaseUrl` указывает на правильный адрес:
     - Docker: `http://localhost:8080`
     - Локально: `http://localhost:5281`

3. **CORS настройки:**
   - API должен разрешать запросы с Client
   - Проверьте логи API на наличие ошибок CORS

### Проблема 5: Ошибки миграций

**Решения:**

1. **PostgreSQL не запущен:**
   ```bash
   docker compose ps postgres
   docker compose up -d postgres
   ```

2. **Неправильный connection string:**
   - Проверьте `appsettings.json` или `appsettings.Development.json`
   - Убедитесь, что порт правильный (5433 для Docker)

3. **База данных не существует:**
   ```bash
   docker compose exec postgres psql -U postgres -c "CREATE DATABASE learning_platform;"
   ```

4. **Пересоздание базы:**
   ```bash
   docker compose down -v
   docker compose up -d postgres
   # Подождите 10-20 секунд
   dotnet ef database update --project LearningPlatform.Data --startup-project LearningPlatform.API
   ```

### Проблема 6: Swagger показывает ошибку "Unable to render this definition"

**Решения:**

1. **Перезапустите API:**
   ```bash
   docker compose restart api
   ```

2. **Очистите кэш браузера:**
   - Откройте Swagger UI в режиме инкогнито
   - Или очистите кэш браузера (Ctrl+Shift+Delete)

3. **Проверьте версию пакета Microsoft.OpenApi:**
   - Убедитесь, что установлена версия 2.3.0 или выше

### Проблема 7: Навигация не работает после отправки формы

**Решения:**

1. **Проверьте логи:**
   - Откройте консоль браузера (F12)
   - Проверьте наличие ошибок JavaScript
   - Проверьте логи сервера

2. **Убедитесь, что используется forceLoad:**
   - В коде навигации должен быть `forceLoad: true` для полной перезагрузки страницы

3. **Проверьте StateHasChanged:**
   - Убедитесь, что вызывается `StateHasChanged()` после асинхронных операций

## 🔍 Проверка работоспособности

### Проверка API

```bash
# Проверить доступность API
curl http://localhost:8080

# Проверить Swagger
curl http://localhost:8080/swagger/v1/swagger.json | head -20
```

### Проверка базы данных

```bash
# Подключиться к БД
docker compose exec postgres psql -U postgres -d learning_platform

# Проверить таблицы
\dt

# Проверить пользователей
SELECT id, email, role FROM "Users";

# Выйти
\q
```

### Проверка логов

```bash
# Логи API
docker compose logs api --tail=50

# Логи Client
docker compose logs client --tail=50

# Логи всех сервисов
docker compose logs --tail=50
```

## 📚 Следующие шаги

После успешного запуска:

1. Изучите [README.md](./README.md) для полного описания проекта
2. Ознакомьтесь с [DOCKER.md](./DOCKER.md) для работы с Docker
3. Прочитайте [CLEAR_STORAGE.md](./CLEAR_STORAGE.md) для управления данными аутентификации
4. Изучите API через Swagger UI: http://localhost:8080/swagger

## 💡 Полезные команды

```bash
# Просмотр всех контейнеров
docker compose ps

# Просмотр логов в реальном времени
docker compose logs -f

# Перезапуск сервиса
docker compose restart api

# Остановка всех сервисов
docker compose down

# Полная очистка (удалит данные БД!)
docker compose down -v
```

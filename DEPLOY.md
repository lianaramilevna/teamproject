# Развертывание проекта на новом компьютере

Пошаговая инструкция по запуску Learning Platform на новом компьютере через Docker.

## 📋 Требования

Перед началом убедитесь, что на новом компьютере установлено:

- **Docker** версии 20.10 или выше
- **Docker Compose** версии 2.0 или выше
- **.NET SDK 10.0** (только для применения миграций, если не используете Docker для миграций)
- **Git** (если клонируете проект из репозитория)

### Проверка установки

```bash
# Проверить Docker
docker --version
# Должно показать: Docker version 20.10.x или выше

# Проверить Docker Compose
docker compose version
# Должно показать: Docker Compose version v2.x.x или выше

# Проверить .NET (опционально, только для миграций)
dotnet --version
# Должно показать: 10.0.x или выше
```

## 🚀 Быстрый старт

### Шаг 1: Получение проекта

#### Вариант A: Клонирование из Git репозитория

```bash
git clone <repository-url>
cd team_learn
```

#### Вариант B: Копирование проекта

Скопируйте всю папку проекта на новый компьютер (через USB, сеть, архив и т.д.)

### Шаг 2: Переход в директорию проекта

```bash
cd /path/to/team_learn
```

### Шаг 3: Запуск всех сервисов

```bash
# Запустить все сервисы (PostgreSQL, API, Client, BackgroundTasks)
docker compose up -d
```

Дождитесь запуска контейнеров (обычно 30-60 секунд).

Проверить статус:
```bash
docker compose ps
```

Все сервисы должны быть в статусе `Up`.

### Шаг 4: Применение миграций базы данных

**✅ Миграции применяются автоматически!**

При первом запуске API автоматически:
1. Применяет все pending миграции базы данных
2. Создает начальных пользователей (если их еще нет)

**Начальные пользователи:**
- **Студент**: `tst@tst.com` / `qweasd`
- **Инструктор**: `inst@inst.com` / `qweasd`

Вы можете сразу войти в систему используя эти учетные данные.

**Примечание:** Если по какой-то причине миграции не применились автоматически, вы можете применить их вручную:

#### Вариант A: Локально (если установлен .NET SDK)

```bash
# Установить EF Core Tools (если еще не установлены)
dotnet tool install --global dotnet-ef

# Добавить в PATH
export PATH="$PATH:$HOME/.dotnet/tools"

# Применить миграции
dotnet ef database update --project LearningPlatform.Data --startup-project LearningPlatform.API
```

**Примечание:** При выполнении миграций вы можете увидеть `HostAbortedException` - это нормально, миграции применяются успешно.

#### Вариант B: Через Docker контейнер

```bash
# Войти в контейнер API
docker compose exec api bash

# Внутри контейнера
export PATH="$PATH:$HOME/.dotnet/tools"
dotnet ef database update --project /src/LearningPlatform.Data --startup-project /src/LearningPlatform.API

# Выйти из контейнера
exit
```

### Шаг 5: Проверка работоспособности

1. **Проверьте API:**
   - Откройте http://localhost:8080/swagger
   - Должен открыться Swagger UI

2. **Проверьте Client:**
   - Откройте http://localhost:5002
   - Должна открыться главная страница

3. **Проверьте логи:**
   ```bash
   docker compose logs --tail=50
   ```

## ⚙️ Настройка окружения

### Переменные окружения

Все настройки находятся в `docker-compose.yml`. При необходимости вы можете изменить:

#### PostgreSQL

```yaml
services:
  postgres:
    environment:
      POSTGRES_USER: postgres        # Измените на свой
      POSTGRES_PASSWORD: postgres    # Измените на безопасный пароль
      POSTGRES_DB: learning_platform
```

#### API

```yaml
services:
  api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development  # или Production
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=learning_platform;Username=postgres;Password=postgres
```

**Важно:** Если изменили пароль PostgreSQL, обновите connection string в API и BackgroundTasks.

#### Client

```yaml
services:
  client:
    environment:
      - ApiBaseUrl=http://api:8080  # Внутри Docker сети
```

Для доступа с хоста используйте `http://localhost:8080` (если API доступен на хосте).

### Изменение портов

Если порты заняты, измените их в `docker-compose.yml`:

```yaml
services:
  api:
    ports:
      - "8081:8080"  # Измените 8080 на 8081

  client:
    ports:
      - "5003:5000"  # Измените 5002 на 5003
      - "5004:5001"  # Измените 5003 на 5004

  postgres:
    ports:
      - "5434:5432"  # Измените 5433 на 5434
```

Не забудьте обновить `ApiBaseUrl` в Client, если изменили порт API.

## 🔧 Дополнительные настройки

### JWT Secret Key

Для production окружения измените JWT secret key в `LearningPlatform.API/appsettings.json`:

```json
{
  "Jwt": {
    "Key": "your-very-long-and-secure-secret-key-minimum-32-characters"
  }
}
```

Или используйте переменную окружения:

```yaml
services:
  api:
    environment:
      - Jwt__Key=your-very-long-and-secure-secret-key-minimum-32-characters
```

### Application Insights

Для мониторинга в Azure добавьте connection string:

```yaml
services:
  api:
    environment:
      - ApplicationInsights__ConnectionString=your-connection-string
```

## 📊 Проверка работоспособности

### Проверка контейнеров

```bash
# Статус всех контейнеров
docker compose ps

# Должны быть запущены:
# - learningplatform-postgres (Up)
# - learningplatform-api (Up)
# - learningplatform-client (Up)
# - learningplatform-backgroundtasks (Up)
```

### Проверка логов

```bash
# Все сервисы
docker compose logs --tail=50

# Конкретный сервис
docker compose logs api --tail=50
docker compose logs client --tail=50
```

### Проверка базы данных

```bash
# Подключиться к PostgreSQL
docker compose exec postgres psql -U postgres -d learning_platform

# Проверить таблицы
\dt

# Должны быть таблицы:
# - Users
# - Courses
# - Assignments
# - Submissions
# - CourseMemberships
# - Teams
# - TeamMemberships
# - __EFMigrationsHistory

# Выйти
\q
```

### Проверка API

```bash
# Проверить доступность API
curl http://localhost:8080

# Проверить Swagger JSON
curl http://localhost:8080/swagger/v1/swagger.json | head -20
```

## 🐛 Решение проблем

### Проблема: Порты заняты

**Решение:**
1. Найдите, какой процесс использует порт:
   ```bash
   # Linux/Mac
   lsof -i :8080
   # Windows
   netstat -ano | findstr :8080
   ```

2. Остановите процесс или измените порты в `docker-compose.yml`

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Проверить логи
docker compose logs

# Пересобрать образы
docker compose build --no-cache

# Запустить заново
docker compose up -d
```

### Проблема: Ошибки подключения к БД

**Решение:**
1. Проверьте, что PostgreSQL запущен:
   ```bash
   docker compose ps postgres
   ```

2. Проверьте connection string в `docker-compose.yml`

3. Проверьте логи PostgreSQL:
   ```bash
   docker compose logs postgres
   ```

### Проблема: Миграции не применяются автоматически

**Решение:**
1. Проверьте логи API:
   ```bash
   docker compose logs api --tail=50
   ```
   Должны быть строки: "Applying database migrations..." и "Database migrations applied successfully"

2. Убедитесь, что PostgreSQL запущен и готов:
   ```bash
   docker compose ps postgres
   ```

3. Проверьте connection string в `docker-compose.yml`

4. Если миграции не применились автоматически, примените их вручную:
   ```bash
   docker compose exec api bash
   # Внутри контейнера
   export PATH="$PATH:$HOME/.dotnet/tools"
   dotnet ef database update --project /src/LearningPlatform.Data --startup-project /src/LearningPlatform.API
   exit
   ```

5. Перезапустите API:
   ```bash
   docker compose restart api
   ```

### Проблема: Client не может подключиться к API

**Решение:**
1. Проверьте `ApiBaseUrl` в `docker-compose.yml` для Client
2. Убедитесь, что API запущен: `docker compose ps api`
3. Проверьте логи Client: `docker compose logs client`

## 🔄 Обновление проекта

Если проект обновился (новые миграции, изменения кода):

```bash
# Остановить контейнеры
docker compose down

# Обновить код (если из Git)
git pull

# Пересобрать образы
docker compose build --no-cache

# Запустить заново
docker compose up -d

# Применить новые миграции
dotnet ef database update --project LearningPlatform.Data --startup-project LearningPlatform.API
```

## 🗑️ Полная очистка и переустановка

Если нужно начать с чистого листа:

```bash
# Остановить и удалить все контейнеры, volumes и сети
docker compose down -v

# Удалить образы
docker compose down --rmi all

# Очистить неиспользуемые ресурсы Docker
docker system prune -a --volumes

# Затем снова запустить
docker compose up -d --build
dotnet ef database update --project LearningPlatform.Data --startup-project LearningPlatform.API
```

## 📝 Полезные команды

```bash
# Просмотр логов в реальном времени
docker compose logs -f

# Перезапуск конкретного сервиса
docker compose restart api

# Остановка всех сервисов
docker compose stop

# Запуск остановленных сервисов
docker compose start

# Просмотр использования ресурсов
docker stats

# Просмотр всех образов проекта
docker images | grep learningplatform

# Просмотр всех volumes
docker volume ls | grep learningplatform
```

## 🌐 Доступ к приложению

После успешного запуска:

- **API Swagger**: http://localhost:8082/swagger (или другой порт, если вы его изменили)
- **Blazor Client**: http://localhost:5002
- **PostgreSQL**: localhost:5433 (если нужен прямой доступ)

## 🔐 Первый запуск

После первого запуска:

1. Откройте http://localhost:5002
2. **Войдите используя начальные учетные данные:**
   - **Студент**: `tst@tst.com` / `qweasd`
   - **Инструктор**: `inst@inst.com` / `qweasd`
3. Или зарегистрируйтесь как новый студент
4. Создайте первый курс (если вы Instructor)
5. Начните использовать платформу!

**Примечание:** Начальные пользователи создаются автоматически при первом запуске, если база данных пуста.

## 📚 Дополнительная документация

- [README.md](./README.md) - Общее описание проекта
- [DOCKER.md](./DOCKER.md) - Подробная инструкция по Docker
- [QUICKSTART.md](./QUICKSTART.md) - Быстрый старт и решение проблем

# Как очистить данные в браузере

Данные аутентификации сохраняются в `localStorage` браузера и не очищаются автоматически при перезапуске контейнеров или сервера.

## 🎯 Зачем это нужно?

Очистка данных localStorage может понадобиться в следующих случаях:

- Тестирование с разными пользователями
- Сброс состояния аутентификации
- Решение проблем с устаревшими токенами
- Переключение между окружениями (Development/Production)

## 🔧 Способ 1: Через консоль браузера (DevTools) - Рекомендуется

Самый быстрый способ очистки данных.

### Шаги:

1. Откройте консоль разработчика:
   - **Chrome/Edge**: `F12` или `Ctrl+Shift+I` (Windows/Linux) / `Cmd+Option+I` (Mac)
   - **Firefox**: `F12` или `Ctrl+Shift+I` (Windows/Linux) / `Cmd+Option+I` (Mac)
   - **Safari**: `Cmd+Option+I` (нужно включить Developer menu в настройках)

2. Перейдите на вкладку **Console**

3. Выполните одну из команд:

```javascript
// Очистить только данные аутентификации Learning Platform
localStorage.removeItem('authData');

// Или использовать встроенную функцию (если доступна)
clearAuthData();

// Очистить ВСЕ данные localStorage (включая другие приложения на этом домене)
localStorage.clear();
```

4. Перезагрузите страницу (`F5` или `Ctrl+R` / `Cmd+R`)

После выполнения команды вы будете автоматически перенаправлены на страницу входа.

## 🗂 Способ 2: Через DevTools → Application/Storage

Визуальный способ очистки через интерфейс браузера.

### Для Chrome/Edge:

1. Откройте DevTools (`F12`)
2. Перейдите на вкладку **Application**
3. В левой панели найдите **Local Storage**
4. Раскройте и выберите ваш домен (например, `http://localhost:5002`)
5. Найдите ключ `authData` в правой панели
6. Нажмите правой кнопкой мыши на `authData` и выберите **Delete**
7. Или нажмите правой кнопкой на домен и выберите **Clear** для полной очистки

### Для Firefox:

1. Откройте DevTools (`F12`)
2. Перейдите на вкладку **Storage**
3. В левой панели найдите **Local Storage**
4. Раскройте и выберите ваш домен
5. Найдите ключ `authData` и удалите его
6. Или используйте кнопку **Clear All** для полной очистки

### Для Safari:

1. Откройте Developer Tools (`Cmd+Option+I`)
2. Перейдите на вкладку **Storage**
3. В левой панели найдите **Local Storage**
4. Выберите ваш домен
5. Найдите и удалите ключ `authData`

## 🚪 Способ 3: Через кнопку "Logout"

Самый простой способ для пользователей.

1. Войдите в систему
2. Нажмите на ваш email в правом верхнем углу навигационной панели
3. Выберите **Logout** из выпадающего меню
4. Данные аутентификации будут автоматически очищены

**Примечание:** Кнопка Logout доступна только для аутентифицированных пользователей.

## 💻 Способ 4: Программно (для разработки)

Если вы разрабатываете или тестируете приложение, можно использовать программные методы.

### В Blazor компоненте:

```csharp
@inject AuthStateService AuthStateService

// Очистить только authData
await AuthStateService.ClearAuth();

// Очистить весь localStorage
await AuthStateService.ClearAllStorage();
```

### Через JavaScript Interop:

```csharp
@inject IJSRuntime JSRuntime

// Очистить authData
await JSRuntime.InvokeVoidAsync("localStorage.removeItem", "authData");

// Очистить весь localStorage
await JSRuntime.InvokeVoidAsync("localStorage.clear");
```

## 🔍 Проверка данных в localStorage

Чтобы посмотреть, что хранится в localStorage:

### Через консоль браузера:

```javascript
// Показать все ключи
Object.keys(localStorage);

// Показать значение authData
localStorage.getItem('authData');

// Показать все данные в удобном формате
JSON.parse(localStorage.getItem('authData') || '{}');
```

### Через DevTools:

1. Откройте DevTools (`F12`)
2. Перейдите на вкладку **Application** (Chrome) или **Storage** (Firefox)
3. В левой панели найдите **Local Storage**
4. Выберите ваш домен
5. Вы увидите все ключи и их значения в правой панели

## 📦 Структура данных authData

Ключ `authData` содержит JSON с следующей структурой:

```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "guid",
    "email": "user@example.com",
    "role": 0
  },
  "expiresAt": "2025-12-14T15:30:00Z"
}
```

Где:
- `token` - JWT токен для аутентификации
- `user` - данные текущего пользователя
- `expiresAt` - время истечения токена (ISO 8601)

## ⚠️ Важные замечания

1. **Очистка localStorage влияет только на текущий домен:**
   - `http://localhost:5002` и `http://localhost:5003` - разные домены
   - Данные не синхронизируются между разными портами

2. **Данные сохраняются между сессиями:**
   - Даже после закрытия браузера данные остаются
   - Для полной очистки нужно явно удалить данные

3. **Очистка всего localStorage:**
   - Команда `localStorage.clear()` удалит ВСЕ данные для этого домена
   - Это может повлиять на другие приложения, использующие localStorage

4. **В режиме инкогнито:**
   - Данные localStorage очищаются автоматически при закрытии окна
   - Полезно для тестирования без очистки данных

## 🧪 Тестирование после очистки

После очистки данных:

1. Перезагрузите страницу
2. Вы должны быть перенаправлены на страницу входа (`/login`)
3. Попробуйте войти снова
4. Проверьте, что данные сохраняются после входа

## 🔄 Автоматическая очистка

Приложение автоматически очищает данные в следующих случаях:

- При нажатии кнопки "Logout"
- При истечении срока действия токена (если реализовано)
- При получении ошибки 401 (Unauthorized) от API

## 📝 Связанные файлы

- `LearningPlatform.Client/wwwroot/authState.js` - JavaScript функции для работы с localStorage
- `LearningPlatform.Client/Services/AuthStateService.cs` - C# сервис для управления состоянием аутентификации
- `LearningPlatform.Client/wwwroot/clearStorage.js` - Утилиты для очистки storage

## 💡 Полезные советы

1. **Используйте режим инкогнито для тестирования:**
   - Данные не сохраняются между сессиями
   - Удобно для тестирования разных пользователей

2. **Создайте закладку для быстрой очистки:**
   ```javascript
   javascript:localStorage.removeItem('authData');location.reload();
   ```

3. **Используйте расширения браузера:**
   - Некоторые расширения позволяют быстро очищать localStorage
   - Например, "EditThisCookie" или "Local Storage Manager"

4. **Для разработки:**
   - Добавьте кнопку "Clear Auth" в интерфейс разработки
   - Или используйте горячие клавиши для очистки
